import { Provider } from 'ethers/providers';
import { DbContext } from '../db/db';
import { SpockConfig } from './config';
import { Services, TransactionalServices } from './types';
export declare function createServices(config: SpockConfig, db: DbContext): Promise<Services>;
export declare function withTx<T>(services: Services, op: (tx: TransactionalServices) => Promise<T>): Promise<T>;
export declare function createProvider(url: string, retries: number): Provider;
