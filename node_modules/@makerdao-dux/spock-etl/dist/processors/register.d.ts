import { Services } from '../services/types';
import { Processor } from './types';
/**
 * Prepares vulcan2x.job table with all processors. Note: this should be called just once per ETL start!
 */
export declare function registerProcessors(services: Services, processors: Processor[]): Promise<void>;
export declare function registerChain(services: Services): Promise<void>;
