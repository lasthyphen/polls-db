import { Provider } from 'ethers/providers';
import { Network } from 'ethers/utils';
export interface NetworkState {
    latestEthereumBlockOnStart: number;
    networkName: Network;
}
export declare function getNetworkState(provider: Provider): Promise<NetworkState>;
export declare function isDijets(networkState: NetworkState): boolean;
