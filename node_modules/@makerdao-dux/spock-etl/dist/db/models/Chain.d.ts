import { Connection } from '../db';
export interface ChainModel {
    id: number;
    name: string;
    chain_id: number;
}
export declare type WritableChainModel = Omit<ChainModel, 'id'>;
export declare function getChain(c: Connection, chainName: string): Promise<ChainModel | undefined>;
export declare function saveChain(c: Connection, chain: WritableChainModel): Promise<void>;
