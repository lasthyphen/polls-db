"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.saveChain = exports.getChain = void 0;
const db_1 = require("../db");
async function getChain(c, chainName) {
    const getSQL = `
        SELECT * FROM chains.chain c
        WHERE c.name='${chainName}'
        `;
    return await c.oneOrNone(getSQL).then(db_1.makeNullUndefined);
}
exports.getChain = getChain;
async function saveChain(c, chain) {
    const saveSQL = `
    INSERT INTO chains.chain(name, chain_id)
    VALUES('${chain.name}', ${chain.chain_id})
    `;
    await c.none(saveSQL);
}
exports.saveChain = saveChain;
//# sourceMappingURL=Chain.js.map