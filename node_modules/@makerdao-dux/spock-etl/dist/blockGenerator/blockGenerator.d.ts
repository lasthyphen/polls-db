import { Services } from '../services/types';
export declare class BlockGenerator {
    private readonly services;
    constructor(services: Services);
    private connection;
    private schema;
    init(): Promise<void>;
    deinit(): Promise<void>;
    run(fromBlockNo: number, toBlockNo?: number): Promise<void>;
}
