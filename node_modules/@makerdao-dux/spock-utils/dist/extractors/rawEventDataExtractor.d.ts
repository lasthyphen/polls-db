import { BlockModel } from '@makerdao-dux/spock-etl/dist/db/models/Block';
import { BlockExtractor } from '@makerdao-dux/spock-etl/dist/processors/types';
import { LocalServices, TransactionalServices } from '@makerdao-dux/spock-etl/dist/services/types';
import { Log } from 'ethers/providers';
export interface SimpleProcessorDefinition {
    address: string;
    startingBlock?: number;
}
export declare function makeRawLogExtractors(_extractors: (string | SimpleProcessorDefinition)[]): BlockExtractor[];
export declare function extractRawLogs(services: TransactionalServices, blocks: BlockModel[], address: string | string[]): Promise<PersistedLog[]>;
export declare function getPersistedLogs(services: LocalServices, addresses: string[], blocks: BlockModel[]): Promise<any[]>;
export declare function getExtractorName(address: string): string;
export interface PersistedLog {
    block_id: number;
    tx_id: number;
    log_index: number;
    address: string;
    data: string;
    topics: string;
}
export declare function getLogs(services: TransactionalServices, blocks: BlockModel[], address: string[] | string): Promise<Log[]>;
