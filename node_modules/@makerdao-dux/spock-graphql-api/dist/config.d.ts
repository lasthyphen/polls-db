import { Env } from '@makerdao-dux/spock-etl/dist/services/configUtils';
export interface ApiConfig {
    api: {
        whitelisting: {
            enabled: boolean;
            whitelistedQueriesDir?: string;
            bypassSecret?: string;
        };
        responseCaching: {
            enabled: boolean;
            duration: string;
            transformKey: (key: string) => string;
        };
        port: number;
    };
    db: {
        database: string;
        user: string;
        password: string;
        host: string;
        port: number;
    };
}
export declare function getConfig(env: Env, configPath: string): ApiConfig;
